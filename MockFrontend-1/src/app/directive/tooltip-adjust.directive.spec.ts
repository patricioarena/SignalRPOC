import { TooltipAdjustDirective } from './directive/tooltip-adjust.directive';

describe('TooltipAdjustDirective', () => {
  it('should create an instance', () => {
    const directive = new TooltipAdjustDirective();
    expect(directive).toBeTruthy();
  });
});
